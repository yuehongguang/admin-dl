package ${package}.mapper;

import ${package}.admin.entity.${className};
import tk.mybatis.mapper.common.Mapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends Mapper<${className}> {
	
}
